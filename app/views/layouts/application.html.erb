<!DOCTYPE html>
<html>
<head>
  <title>BlogApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
 
  <%= javascript_include_tag "application", "comments", "tunnel"%>
  <%# javascript_include_tag :defaults %>
  <%= csrf_meta_tags %>
  	 <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.2.14/angular.js"></script>
	 <script src= "https://ajax.googleapis.com/ajax/libs/angularjs/1.2.14/angular-route.js"></script>
	 <script src= "https://ajax.googleapis.com/ajax/libs/angularjs/1.2.14/angular-resource.js"></script>
</head>
   <% if current_user  %>
<body ng-app="Sampleblog">
	12345678
	<% else %>
	<body>
	<%end%>

	
  <div ng-controller="MyController" >
      <% if current_user  %>
		<h3>Welcome {{ useremail }} </h3>
	<% end %>
  </div>


<script>
  sampleApp.controller("MyController", function($scope) {
   $scope.useremail = " <%= current_user.email if current_user %> ";
 });
 
</script>
	
	

	 
 <p class="notice"><% notice %></p>
 <p class="alert"><% alert %></p>

 <ul>
<%= render 'devise/shared/navigation' %>
</ul>


<% flash.each do |name, msg| %>
<%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
<% end %>

<div ng-view>
<%= yield %>
</div>

    <!-- script type="text/javascript">
    /* * * CONFIGURATION VARIABLES: EDIT BEFORE PASTING INTO YOUR WEBPAGE * * */
    var disqus_shortname = 'localhost3000'; // required: replace example with your forum shortname

    /* * * DON'T EDIT BELOW THIS LINE * * */
    (function () {
        var s = document.createElement('script'); s.async = true;
        s.type = 'text/javascript';
        s.src = '//' + disqus_shortname + '.disqus.com/count.js';
        (document.getElementsByTagName('HEAD')[0] || document.getElementsByTagName('BODY')[0]).appendChild(s);
    }());
    </script -->
</body>
</html>
